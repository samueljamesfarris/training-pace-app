node_modules
dist
.DS_Store
*.local

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Machine-specific Claude Code settings (launch.json is shared on purpose)
.claude/settings.local.json

# Real GPS logs are location data — keep them out of the public repo
tests/logs/
